<nav class="navbar navbar-expand-lg bg-dark border-bottom border-body" data-bs-theme="dark">
  <div class="container px-5">
    <%= link_to root_path, class: 'navbar-brand' do %>
      <i class="fa-brands fa-x-twitter fa-2xl" style="color: #ffffff;"></i>
    <% end %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0 pe-3">
        <% if current_page?(controller: 'tweets', action: 'index') %>
          <li class="nav-item"><%= link_to 'Inicio', root_path, class: 'nav-link active' %></li>
        <% else %>
          <li class="nav-item"><%= link_to 'Inicio', root_path, class: 'nav-link ver' %></li>
        <% end %>
        <li class="nav-item">
          <%= link_to 'Tweets', new_tweet_path, class: 'nav-link' %>
        </li>
      </ul>
      <%= form_with url: tweets_path, method: :get, class:"d-flex", role:"search" do |form|%>
        <%= form.text_field :query_text, placeholder: 'Buscar tweet', value: params[:query_text], class:'form-control ps-2'%>
        <button class="btn btn-sm btn-primary ms-1" type="submit">Buscar</button>
      <%end%>
      <form class="d-flex" role="search">
        <%# <input class="form-control me-2" type="search" placeholder="Buscar" aria-label="Search"> %>
      </form>
    </div>
  </div>
</nav>
