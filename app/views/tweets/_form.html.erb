<%= form_with(model: tweet) do |form| %>
  <% if tweet.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tweet.errors.count, "error") %> prohibited this tweet from being saved:</h2>
      <ul>
        <% tweet.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= form.label :descripcion, style: "display: block", class:"form-label" %>
    <%= form.text_area :description, class:"form-control" %>
  </div>
  <div>
    <%= form.label :Usuario, style: "display: block", class:"form-label mt-2" %>
    <%= form.text_field :userName,class:"form-control w-25" %>
  </div>
  <div>
    <%= form.submit :Twittear, class:"btn btn-primary mt-3"%>
  </div>
<% end %>
